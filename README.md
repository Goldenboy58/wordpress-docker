# WordPress Docker Setup

This repository contains a Docker Compose configuration to run **WordPress** with a **MySQL database** locally. It’s designed for quick setup, testing, and development.

## 🚀 Getting Started

### Prerequisites
- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/)

### Installation
1. Clone this repository:
   ```bash
   git clone https://github.com/Goldenboy58/wordpress-docker.git
   cd wordpress-docker

git add README.md
git commit -m "Update README with full documentation"
git push
# WordPress + MySQL with Docker Compose

This repository demonstrates how to run **WordPress** with a **MySQL database** using Docker Compose.  
Each service has its own `docker-compose.yml` file, and both are connected through a shared Docker network.

---

## 🚀 Getting Started

### Prerequisites
- [Docker Desktop](https://docs.docker.com/get-docker/) with WSL 2 integration enabled
- [Docker Compose](https://docs.docker.com/compose/)

---

## 📂 Project Structure
# WordPress + MySQL with Docker Compose

## 🚀 Quickstart
```bash
docker-compose up -d
